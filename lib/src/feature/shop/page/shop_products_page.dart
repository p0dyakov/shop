import 'package:flutter/material.dart';

class ShopProductsPage extends StatelessWidget {
  const ShopProductsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
        body: Container(),
      );
}
